# Copy to .env and fill in. .env is gitignored — never commit it.
#
# ElevenLabs Scribe does all transcription (word-level timestamps, speaker
# diarization, filler-word tagging). Without this key nothing transcribes.
# Get one at: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
